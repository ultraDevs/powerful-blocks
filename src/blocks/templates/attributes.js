import { __ } from '@wordpress/i18n';

const attributes = {
	blockId: {
		type: 'string',
		default: undefined,
	},
	
	showModal: {
		type: 'boolean',
		// default: false,
	}
};

export default attributes;
